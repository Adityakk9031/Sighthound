(
    rules: [
        // Search mode rules for SQL injection patterns
        (
            id: Some("python-sql-injection-001"),
            name: Some("SQL Injection via String Formatting"),
            category: Some("injection"),
            mode: "search",
            patterns: Some([
                "execute.*%.*",
                "execute.*format.*",
                "execute.*\\.format",
                "executemany.*%.*",
                "executemany.*format.*",
            ]),
            finding_type: Some("SQL Injection"),
            severity: Some("High"),
            confidence: Some("Medium"),
            description: Some("SQL query constructed using string formatting, which may be vulnerable to injection"),
            file_types: Some((
                extensions: Some([".py"]),
                include_patterns: None,
                exclude_patterns: None,
            )),
            tags: Some(["sql", "injection", "database"]),
            pattern: None,
            sources: None,
            sinks: None,
            propagators: None,
            sanitizers: None,
            conditions: None,
            message: None,
        ),
        (
            id: Some("python-sql-injection-002"), 
            name: Some("SQL Injection via String Concatenation"),
            category: Some("injection"),
            mode: "search",
            patterns: Some([
                "execute.*\\+.*",
                "executemany.*\\+.*",
                "query.*\\+.*",
            ]),
            finding_type: Some("SQL Injection"),
            severity: Some("High"), 
            confidence: Some("Medium"),
            description: Some("SQL query constructed using string concatenation, which may be vulnerable to injection"),
            file_types: Some((
                extensions: Some([".py"]),
                include_patterns: None,
                exclude_patterns: None,
            )),
            tags: Some(["sql", "injection", "database"]),
            pattern: None,
            sources: None,
            sinks: None,
            propagators: None,
            sanitizers: None,
            conditions: None,
            message: None,
        ),
        (
            id: Some("python-sql-injection-003"),
            name: Some("Raw SQL Execution with Variables"), 
            category: Some("injection"),
            mode: "search",
            patterns: Some([
                "cursor\\.execute\\(",
                "connection\\.execute\\(",
                "db\\.execute\\(",
                "session\\.execute\\(",
            ]),
            finding_type: Some("SQL Injection"),
            severity: Some("Medium"),
            confidence: Some("Low"),
            description: Some("Raw SQL execution detected - verify parameterized queries are used"),
            file_types: Some((
                extensions: Some([".py"]),
                include_patterns: None,
                exclude_patterns: None,
            )),
            tags: Some(["sql", "execution", "database"]),
            pattern: None,
            sources: None,
            sinks: None,
            propagators: None,
            sanitizers: None,
            conditions: None,
            message: None,
        ),
        
        // Taint mode rules for SQL injection data flows
        (
            id: Some("python-sql-taint-001"),
            name: Some("SQL Injection Taint Flow"),
            category: Some("injection"), 
            mode: "taint",
            sources: Some([
                "request.*",
                "request\\.args\\.",
                "request\\.form\\.",
                "request\\.json\\.",
                "request\\.data",
                "request\\.values\\.",
                "input\\(",
                "sys\\.argv",
                "os\\.environ",
                "flask\\.request",
                "django\\.request",
            ]),
            sinks: Some([
                "execute\\(",
                "executemany\\(",
                "cursor\\.execute",
                "connection\\.execute", 
                "db\\.execute",
                "session\\.execute",
                "query\\(",
                "raw\\(",
                "extra\\(",
            ]),
            sanitizers: Some([
                "escape\\(",
                "quote\\(",
                "sanitize\\(",
                "prepare\\(",
                "bind\\(",
                "param\\(",
            ]),
            finding_type: Some("SQL Injection via Taint Flow"),
            severity: Some("Critical"),
            confidence: Some("High"),
            description: Some("User input flows directly to SQL execution without proper sanitization"),
            file_types: Some((
                extensions: Some([".py"]),
                include_patterns: None,
                exclude_patterns: None,
            )),
            tags: Some(["sql", "injection", "taint", "database"]),
            pattern: None,
            patterns: None,
            propagators: None,
            conditions: None,
            message: None,
        )
    ]
) 